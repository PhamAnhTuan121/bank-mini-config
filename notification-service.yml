server:
  port: 8084

spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: tuanpaph35819@fpt.edu.vn
    password: zrzd olmd jvzq obwh
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
      mail.smtp.starttls.required: true

  kafka:
    consumer:
      group-id: notification-group-v3
      auto-offset-reset: earliest
      key-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      properties:
        spring.deserializer.key.delegate.class: org.apache.kafka.common.serialization.StringDeserializer
        spring.deserializer.value.delegate.class: org.springframework.kafka.support.serializer.JsonDeserializer
        spring.json.trusted.packages: "com.bank.bank_common.dto.notification.request"
        spring.json.value.default.type: com.bank.bank_common.dto.notification.request.NotificationRequest
